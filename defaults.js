const DEFAULTS = {
    userFollowingFeedFilterSettings: {
        showImagePosts: true,
        showPolls: true,
        showTextThreads: true,
        showImageThreads: true
    }
}

module.exports = DEFAULTS;